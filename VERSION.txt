1.1 --> se añadió uso de cursores y el motor de almacenamiento ha sido rediseñado.
1.1.1 --> se agrego opción de cambiar nombre a base de datos.

1.2 --> Se agregaron funciones select sum, max, min y avg y opcion de escoger la carpeta en donde
se alojará la base de datos.
		Se añadieron clases para insert multihilo, por ahora hay problemas con que no todos los elementos se insertan 			asi que no serán ocupadas.
